class MagicBarrierTile extends ImageObject {
    constructor(name, fileName, x, y, width, height) {
        super(name, fileName, x, y, width, height);
    }

    setMovement(direction){

    }
}